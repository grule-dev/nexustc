{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun ./dist/index.mjs",
    "dev": "bun src/index.ts --watch --port=3000",
    "build": "tsdown",
    "check-types": "tsc --noEmit",
    "compile": "bun build ./src/index.ts --outfile ./dist/index.js --target browser --format esm --minify --sourcemap"
  },
  "dependencies": {
    "hono": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@repo/api": "workspace:*",
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/env": "workspace:*"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^22.13.11",
    "tsdown": "^0.16.5",
    "typescript": "^5.8.2"
  }
}
